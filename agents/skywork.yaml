interface:
  display_name: "CloudArc Bounded-Memory Benchmark"
  short_description: "Bounded-memory benchmarks and CI gates for large CloudArc .vibo packages"
  default_prompt: "Use $cloudarc-bounded-memory-benchmark for CloudArc bounded-memory, multi-file dedup, remote telemetry and CI checks."
